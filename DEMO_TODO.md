# FTC-4421 Implementation Checklist
## FinTechCo Payments API — Duplicate Charge Prevention

> This task list was generated by Claude Code from ISSUE.md
> Updated in real-time as work is completed.

---

### Security Audit (Beat 1)
- [x] Scan codebase for hardcoded secrets
- [x] Flag unauthenticated admin endpoints
- [x] Identify SQL injection risk in search endpoint
- [ ] Rotate `PROCESSOR_API_KEY` via secrets manager
- [ ] Add bearer token auth to `/api/admin/payments`
- [ ] Parameterize customer_id query in `/api/payments/search`

### Bug Fix — FTC-4421 (Beat 2)
- [x] Reproduce duplicate charge (cust_A1B2, cust_E5F6)
- [x] Add `idempotency_keys` table with 24-hour TTL
- [x] Update `POST /api/payments` to accept `Idempotency-Key` header
- [x] Return HTTP 200 on duplicate, 201 on first creation
- [x] Add DB index on `created_at` for TTL query performance

### Tests (Beat 2)
- [x] `test_idempotency_key_prevents_duplicate_charge` — PASS
- [x] `test_different_keys_create_separate_charges` — PASS
- [x] `test_no_key_still_works` (backwards compatibility) — PASS
- [x] `test_expired_idempotency_key_creates_new_charge` — PASS
- [ ] Add test for concurrent requests with same key (race condition)
- [ ] Add test for malformed idempotency key format

### Code Review (Beat 3)
- [x] Open PR: feat/duplicate-charge-fix → main
- [ ] Review diff in Claude Code (`/code-review:code-review`)
- [ ] Address any reviewer comments
- [ ] Confirm no PCI-DSS surface area in new DB table
- [ ] Merge after approval

### Governance & Rollout (Beat 4)
- [x] CLAUDE.md committed with team conventions
- [x] PostToolUse hook enforces `pytest` on every file write
- [ ] Add pre-commit hook for secret scanning (gitleaks)
- [ ] Schedule Pilot Week 1 kickoff with 5-engineer cohort
- [ ] Set up metrics baseline (PR cycle time, incidents per sprint)
- [ ] 6-week readout scheduled with Head of DT

### Deployment
- [x] Railway config (`Procfile`, `railway.json`, `runtime.txt`)
- [x] SQLite path fix (`SQLITE_PATH` avoids Railway `DATABASE_URL` collision)
- [ ] `railway up` — deploy feat branch to production
- [ ] Smoke test live URL
- [ ] Update DEMO_PLAYBOOK.md with live Railway URL

---

*Generated and maintained by Claude Code — [claude.ai/claude-code](https://claude.ai/claude-code)*
